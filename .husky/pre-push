#!/bin/sh

# Roda pre-push do frontend e backend em paralelo
npm run pre-push &
pid_front=$!

(cd backend && npm run pre-push) &
pid_back=$!

# Aguarda ambos e captura exit codes
wait $pid_front
exit_front=$?

wait $pid_back
exit_back=$?

# Falha se qualquer um falhar
if [ $exit_front -ne 0 ] || [ $exit_back -ne 0 ]; then
  echo ""
  [ $exit_front -ne 0 ] && echo "❌ Frontend pre-push failed"
  [ $exit_back -ne 0 ]  && echo "❌ Backend pre-push failed"
  exit 1
fi

echo ""
echo "✅ Pre-push passed (frontend + backend)"
